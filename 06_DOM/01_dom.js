// Document Object Model

// const title = document.getElementById('title').id

// console.log(title);

// innerText -> visible text
// textContent -> whole text
// innerHTML -> whole html
// document.querySelector -> will select the first element
// document.querySelectorAll -> will select all the element

// we can't iterate htmlcollection
// we need to convert it to array using Array.from
